# A1. Sistema de Biblioteca - Clase Libro y Autor

## 📋 Descripción de la Actividad

Esta actividad implementa un sistema básico de biblioteca utilizando los conceptos fundamentales de Programación Orientada a Objetos: clases, objetos, atributos y métodos. Se crean dos clases principales: `Libro` y `Autor`, que permiten gestionar la información bibliográfica básica.

## 🎯 Objetivos de Aprendizaje

- Definir clases con atributos y métodos básicos
- Crear instancias de objetos con valores específicos
- Implementar métodos que retornen información formateada
- Utilizar el método `__init__` para inicializar objetos
- Calcular valores derivados a partir de atributos

## 📝 Requerimientos

### Clase `Autor`

**Atributos:**

- `nombre` (string): Nombre completo del autor
- `nacionalidad` (string): País de origen
- `fecha_nacimiento` (string): Fecha en formato "DD/MM/AAAA"

**Métodos:**

- `edad()`: Calcula y retorna la edad del autor
- `presentacion()`: Retorna una presentación formal del autor

### Clase `Libro`

**Atributos:**

- `isbn` (string): Código ISBN único del libro
- `titulo` (string): Título del libro
- `autor` (string): Nombre del autor
- `año_publicacion` (int): Año de publicación
- `disponible` (bool): Estado de disponibilidad (True/False)

**Métodos:**

- `prestar()`: Marca el libro como no disponible
- `devolver()`: Marca el libro como disponible
- `mostrar_info()`: Retorna información completa del libro

## 💻 Solución Completa

```python
# biblioteca.py

from datetime import datetime

class Autor:
    """Clase que representa a un autor de libros"""

    def __init__(self, nombre, nacionalidad, fecha_nacimiento):
        """
        Inicializa un autor con sus datos personales.

        Args:
            nombre (str): Nombre completo del autor
            nacionalidad (str): País de origen
            fecha_nacimiento (str): Fecha en formato "DD/MM/AAAA"
        """
        self.nombre = nombre
        self.nacionalidad = nacionalidad
        self.fecha_nacimiento = fecha_nacimiento

    def edad(self):
        """
        Calcula la edad del autor basándose en su fecha de nacimiento.

        Returns:
            int: Edad en años
        """
        # Convertir string a fecha
        dia, mes, año = map(int, self.fecha_nacimiento.split('/'))
        fecha_nac = datetime(año, mes, dia)

        # Calcular edad
        hoy = datetime.now()
        edad = hoy.year - fecha_nac.year

        # Ajustar si aún no ha cumplido años este año
        if (hoy.month, hoy.day) < (fecha_nac.month, fecha_nac.day):
            edad -= 1

        return edad

    def presentacion(self):
        """
        Genera una presentación formal del autor.

        Returns:
            str: Texto de presentación
        """
        return (f"{self.nombre} es un autor de nacionalidad {self.nacionalidad}, "
                f"nacido el {self.fecha_nacimiento} ({self.edad()} años).")

    def __str__(self):
        """Representación en string del autor"""
        return f"{self.nombre} ({self.nacionalidad})"

    def __repr__(self):
        """Representación técnica del autor"""
        return f"Autor('{self.nombre}', '{self.nacionalidad}', '{self.fecha_nacimiento}')"


class Libro:
    """Clase que representa un libro en la biblioteca"""

    # Contador de libros totales (atributo de clase)
    total_libros = 0

    def __init__(self, isbn, titulo, autor, año_publicacion, disponible=True):
        """
        Inicializa un libro con su información bibliográfica.

        Args:
            isbn (str): Código ISBN único
            titulo (str): Título del libro
            autor (str): Nombre del autor
            año_publicacion (int): Año de publicación
            disponible (bool, optional): Estado inicial. Por defecto True.
        """
        self.isbn = isbn
        self.titulo = titulo
        self.autor = autor
        self.año_publicacion = año_publicacion
        self.disponible = disponible

        # Incrementar contador de libros
        Libro.total_libros += 1

    def prestar(self):
        """
        Marca el libro como prestado (no disponible).

        Returns:
            bool: True si se pudo prestar, False si ya estaba prestado
        """
        if self.disponible:
            self.disponible = False
            print(f"✓ Libro '{self.titulo}' prestado exitosamente.")
            return True
        else:
            print(f"✗ El libro '{self.titulo}' no está disponible.")
            return False

    def devolver(self):
        """
        Marca el libro como devuelto (disponible).

        Returns:
            bool: True si se pudo devolver, False si ya estaba disponible
        """
        if not self.disponible:
            self.disponible = True
            print(f"✓ Libro '{self.titulo}' devuelto exitosamente.")
            return True
        else:
            print(f"✗ El libro '{self.titulo}' ya estaba disponible.")
            return False

    def mostrar_info(self):
        """
        Genera información completa del libro.

        Returns:
            str: Información formateada del libro
        """
        estado = "Disponible ✓" if self.disponible else "Prestado ✗"
        antiguedad = datetime.now().year - self.año_publicacion

        info = f"""
╔══════════════════════════════════════════════════════════════╗
║                    INFORMACIÓN DEL LIBRO                     ║
╚══════════════════════════════════════════════════════════════╝
  ISBN:            {self.isbn}
  Título:          {self.titulo}
  Autor:           {self.autor}
  Año:             {self.año_publicacion} (hace {antiguedad} años)
  Estado:          {estado}
═══════════════════════════════════════════════════════════════
"""
        return info

    def es_antiguo(self):
        """
        Verifica si el libro tiene más de 50 años.

        Returns:
            bool: True si es antiguo, False en caso contrario
        """
        antiguedad = datetime.now().year - self.año_publicacion
        return antiguedad > 50

    def __str__(self):
        """Representación en string del libro"""
        estado = "✓" if self.disponible else "✗"
        return f"{estado} [{self.isbn}] {self.titulo} - {self.autor} ({self.año_publicacion})"

    def __repr__(self):
        """Representación técnica del libro"""
        return f"Libro('{self.isbn}', '{self.titulo}', '{self.autor}', {self.año_publicacion}, {self.disponible})"


# ═══════════════════════════════════════════════════════════════
# PRUEBAS Y DEMOSTRACIÓN
# ═══════════════════════════════════════════════════════════════

def main():
    """Función principal para demostrar el uso de las clases"""

    print("╔═══════════════════════════════════════════════════════════════╗")
    print("║          SISTEMA DE BIBLIOTECA - GESTIÓN DE LIBROS           ║")
    print("╚═══════════════════════════════════════════════════════════════╝\n")

    # ─────────────────────────────────────────────────────────────
    # 1. CREAR AUTORES
    # ─────────────────────────────────────────────────────────────
    print("1. CREANDO AUTORES")
    print("─" * 63)

    autor1 = Autor("Gabriel García Márquez", "Colombia", "06/03/1927")
    autor2 = Autor("Isabel Allende", "Chile", "02/08/1942")
    autor3 = Autor("Jorge Luis Borges", "Argentina", "24/08/1899")

    print(f"✓ Autor 1: {autor1}")
    print(f"✓ Autor 2: {autor2}")
    print(f"✓ Autor 3: {autor3}")

    print(f"\n{autor1.presentacion()}")
    print(f"{autor2.presentacion()}")
    print(f"{autor3.presentacion()}")

    # ─────────────────────────────────────────────────────────────
    # 2. CREAR LIBROS
    # ─────────────────────────────────────────────────────────────
    print("\n\n2. CREANDO LIBROS")
    print("─" * 63)

    libro1 = Libro(
        "978-0307474728",
        "Cien años de soledad",
        "Gabriel García Márquez",
        1967
    )

    libro2 = Libro(
        "978-1501117015",
        "La casa de los espíritus",
        "Isabel Allende",
        1982
    )

    libro3 = Libro(
        "978-0142437223",
        "Ficciones",
        "Jorge Luis Borges",
        1944
    )

    libro4 = Libro(
        "978-0060883287",
        "El amor en los tiempos del cólera",
        "Gabriel García Márquez",
        1985,
        disponible=False  # Este libro ya está prestado
    )

    libros = [libro1, libro2, libro3, libro4]

    print("\nLista de libros en el sistema:")
    for i, libro in enumerate(libros, 1):
        print(f"  {i}. {libro}")

    print(f"\n📚 Total de libros en el sistema: {Libro.total_libros}")

    # ─────────────────────────────────────────────────────────────
    # 3. MOSTRAR INFORMACIÓN DETALLADA
    # ─────────────────────────────────────────────────────────────
    print("\n\n3. INFORMACIÓN DETALLADA DE UN LIBRO")
    print("─" * 63)
    print(libro1.mostrar_info())

    # ─────────────────────────────────────────────────────────────
    # 4. OPERACIONES DE PRÉSTAMO Y DEVOLUCIÓN
    # ─────────────────────────────────────────────────────────────
    print("\n4. OPERACIONES DE PRÉSTAMO Y DEVOLUCIÓN")
    print("─" * 63)

    # Prestar libro disponible
    libro1.prestar()

    # Intentar prestar libro ya prestado
    libro1.prestar()

    # Devolver libro
    libro1.devolver()

    # Intentar devolver libro ya disponible
    libro1.devolver()

    # ─────────────────────────────────────────────────────────────
    # 5. VERIFICAR LIBROS ANTIGUOS
    # ─────────────────────────────────────────────────────────────
    print("\n\n5. VERIFICACIÓN DE LIBROS ANTIGUOS (>50 años)")
    print("─" * 63)

    for libro in libros:
        if libro.es_antiguo():
            print(f"  📜 {libro.titulo} ({libro.año_publicacion}) - LIBRO ANTIGUO")
        else:
            print(f"  📖 {libro.titulo} ({libro.año_publicacion}) - Moderno")

    # ─────────────────────────────────────────────────────────────
    # 6. FILTRAR LIBROS DISPONIBLES
    # ─────────────────────────────────────────────────────────────
    print("\n\n6. LIBROS DISPONIBLES PARA PRÉSTAMO")
    print("─" * 63)

    libros_disponibles = [libro for libro in libros if libro.disponible]
    libros_prestados = [libro for libro in libros if not libro.disponible]

    print(f"\n✓ Disponibles ({len(libros_disponibles)}):")
    for libro in libros_disponibles:
        print(f"  • {libro.titulo} - {libro.autor}")

    print(f"\n✗ Prestados ({len(libros_prestados)}):")
    for libro in libros_prestados:
        print(f"  • {libro.titulo} - {libro.autor}")

    # ─────────────────────────────────────────────────────────────
    # 7. ESTADÍSTICAS
    # ─────────────────────────────────────────────────────────────
    print("\n\n7. ESTADÍSTICAS DEL SISTEMA")
    print("─" * 63)

    tasa_disponibilidad = (len(libros_disponibles) / len(libros)) * 100

    print(f"""
  Total de libros:        {Libro.total_libros}
  Libros disponibles:     {len(libros_disponibles)}
  Libros prestados:       {len(libros_prestados)}
  Tasa disponibilidad:    {tasa_disponibilidad:.1f}%
  Autores únicos:         {len(set(libro.autor for libro in libros))}
    """)

    print("\n╚═══════════════════════════════════════════════════════════════╝\n")


if __name__ == "__main__":
    main()
```

## 🔍 Explicación Detallada del Código

### 1. Clase `Autor`

**Constructor (`__init__`)**

```python
def __init__(self, nombre, nacionalidad, fecha_nacimiento):
    self.nombre = nombre
    self.nacionalidad = nacionalidad
    self.fecha_nacimiento = fecha_nacimiento
```

- Recibe tres parámetros y los asigna como atributos de instancia
- Cada objeto `Autor` tendrá sus propios valores

**Método `edad()`**

```python
def edad(self):
    dia, mes, año = map(int, self.fecha_nacimiento.split('/'))
    fecha_nac = datetime(año, mes, dia)
    hoy = datetime.now()
    edad = hoy.year - fecha_nac.year
    if (hoy.month, hoy.day) < (fecha_nac.month, fecha_nac.day):
        edad -= 1
    return edad
```

- Convierte el string de fecha a objeto `datetime`
- Calcula la diferencia de años
- Ajusta si aún no ha cumplido años este año

**Método `presentacion()`**

- Utiliza f-strings para formatear el texto
- Llama al método `edad()` para incluir la edad calculada

### 2. Clase `Libro`

**Atributo de Clase**

```python
total_libros = 0
```

- Variable compartida entre todas las instancias
- Se incrementa cada vez que se crea un libro

**Constructor**

```python
def __init__(self, isbn, titulo, autor, año_publicacion, disponible=True):
    # ... asignaciones ...
    Libro.total_libros += 1
```

- Parámetro `disponible` tiene valor por defecto `True`
- Incrementa el contador global de libros

**Métodos `prestar()` y `devolver()`**

- Verifican el estado actual antes de cambiar
- Retornan `bool` indicando éxito/fallo
- Imprimen mensajes informativos

**Método `mostrar_info()`**

- Usa caracteres especiales para crear un diseño visual
- Calcula la antigüedad del libro
- Formatea la información de manera clara

**Método `es_antiguo()`**

- Lógica de negocio: considera antiguo si tiene >50 años
- Retorna valor booleano

### 3. Métodos Especiales

**`__str__`**: Representación legible para usuarios

- Usado por `print()`
- Formato conciso y amigable

**`__repr__`**: Representación técnica para desarrolladores

- Muestra cómo recrear el objeto
- Útil para debugging

## 📊 Ejemplo de Salida

```
╔═══════════════════════════════════════════════════════════════╗
║          SISTEMA DE BIBLIOTECA - GESTIÓN DE LIBROS           ║
╚═══════════════════════════════════════════════════════════════╝

1. CREANDO AUTORES
───────────────────────────────────────────────────────────────
✓ Autor 1: Gabriel García Márquez (Colombia)
✓ Autor 2: Isabel Allende (Chile)
✓ Autor 3: Jorge Luis Borges (Argentina)

Gabriel García Márquez es un autor de nacionalidad Colombia, nacido el 06/03/1927 (97 años).
Isabel Allende es un autor de nacionalidad Chile, nacido el 02/08/1942 (82 años).
Jorge Luis Borges es un autor de nacionalidad Argentina, nacido el 24/08/1899 (125 años).


2. CREANDO LIBROS
───────────────────────────────────────────────────────────────

Lista de libros en el sistema:
  1. ✓ [978-0307474728] Cien años de soledad - Gabriel García Márquez (1967)
  2. ✓ [978-1501117015] La casa de los espíritus - Isabel Allende (1982)
  3. ✓ [978-0142437223] Ficciones - Jorge Luis Borges (1944)
  4. ✗ [978-0060883287] El amor en los tiempos del cólera - Gabriel García Márquez (1985)

📚 Total de libros en el sistema: 4
```

## ✅ Puntos Clave para Replicar

1. **Organización del código**: Separar clases, métodos auxiliares y pruebas
2. **Documentación**: Usar docstrings en todas las clases y métodos
3. **Validaciones**: Verificar estados antes de modificar atributos
4. **Formateo**: Usar f-strings para mensajes claros
5. **Métodos especiales**: Implementar `__str__` y `__repr__` para mejor debugging
6. **Atributos de clase**: Usar para datos compartidos (contador global)
7. **Valores por defecto**: Parámetros opcionales con valores sensatos

## 🎓 Conceptos Aplicados

- ✅ Definición de clases
- ✅ Constructor `__init__`
- ✅ Atributos de instancia
- ✅ Atributos de clase
- ✅ Métodos de instancia
- ✅ Métodos especiales (`__str__`, `__repr__`)
- ✅ Encapsulación básica
- ✅ Lógica de negocio en métodos

## 🚀 Extensiones Posibles

1. Agregar clase `Biblioteca` para gestionar múltiples libros
2. Implementar búsqueda por título o autor
3. Agregar fecha de préstamo y devolución
4. Implementar multas por retraso
5. Crear sistema de reservas
6. Persistencia de datos (guardar en archivo)
